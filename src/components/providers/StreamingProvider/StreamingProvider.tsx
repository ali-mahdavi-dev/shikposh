'use client';

import React, { ReactNode } from 'react';

/**
 * Streaming Provider
 * Provides streaming capabilities for Next.js App Router
 * This is mainly for documentation - Next.js handles streaming automatically
 */
export function StreamingProvider({ children }: { children: ReactNode }) {
  // Next.js App Router handles streaming automatically
  // This component is mainly for consistency and future extensibility
  return <>{children}</>;
}

